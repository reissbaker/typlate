{
  "name": "typlate",
  "version": "0.0.7",
  "description": "Typesafe templates for TypeScript",
  "main": "build/index.js",
  "bin": "bin.js",
  "scripts": {
    "build": "rm -rf ./build && tsc && chmod +x ./bin.js && ./bin.js --outdir generated ./examples && tsc",
    "test": "vitest run",
    "test-watch": "vitest"
  },
  "keywords": [
    "templates",
    "types"
  ],
  "author": "@reissbaker",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.2",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^11.0.0",
    "commander": "^11.0.0"
  }
}
