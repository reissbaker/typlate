{
  "name": "typlate",
  "version": "0.0.1",
  "description": "Typesafe templates for TypeScript",
  "main": "build/index.js",
  "bin": "build/index.js",
  "scripts": {
    "test": "rm -rf ./build && tsc && node build/index.js --outdir 'generated' ./examples/ && tsc && vitest"
  },
  "keywords": [
    "templates",
    "types"
  ],
  "author": "@reissbaker",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.2",
    "typescript": "^5.1.3",
    "vitest": "^0.32.2"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^11.0.0",
    "commander": "^11.0.0"
  }
}
